<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    S

    <style name="Theme.OPlay" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/orange_base</item>
        <item name="colorPrimaryVariant">@color/orange_dark</item>
        <item name="colorOnPrimary">@color/white</item>

        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/blue_base</item>
        <item name="colorSecondaryVariant">@color/blue_dark</item>
        <item name="colorOnSecondary">@color/black</item>

        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>

        <!-- Customize your theme here. -->
        <item name="textAppearanceHeadline1">@style/TextAppearance.OPlay.Headline1</item>
        <item name="textAppearanceHeadline2">@style/TextAppearance.OPlay.Headline2</item>
        <item name="textAppearanceHeadline6">@style/TextAppearance.OPlay.Headline6</item>
        <item name="textAppearanceBody1">@style/TextAppearance.OPlay.Body1</item>
        <item name="errorTextAppearance">@style/TextAppearance.OPlay.Error</item>

    </style>

    <style name="Theme.OPlay.NoActionBar" parent="Theme.OPlay">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.OPlay.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" >
        <item name="android:editTextColor">@android:color/white</item>
        <item name="android:textColorHint">@android:color/white</item>
    </style>

    <style name="Theme.OPlay.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>